properties([parameters([string('BOT_IMAGE_NAME')])])

pipeline {
    agent any

    stages {
        stage('Build Bot app') {
            steps {
                sh '''
                   echo "deploy"
                   '''
            }

        }

    }
}